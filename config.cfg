# List of train tracks to activate (comma-separated)
train_tracks=0,1,3,4,5,6,7,8,10

# Train spawn frequency in milliseconds (lower = more frequent)
freight_train_frequency=30000
metro_train_frequency=30000
